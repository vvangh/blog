# 读屏手测路径（M4）

本里程碑验收用 NVDA / VoiceOver / TalkBack 任选其一走通：

1. 打开 `/blog/` → 自动进入某语言首页，听到「vv」一级标题
2. 用跳过链接进入主内容
3. Tab 到主导航，进入一篇分区（技术 / 从零到一）
4. 回到首页，切换主题（浅/暗/自动/自定义），确认 `aria-live` 有反馈且焦点不丢
5. 切换语言，确认 `html[lang]` 与导航文案一致

结果：本地 Playwright + axe（wcag2a/aa）已覆盖首页与分区；人工读屏记录在合并 M5 建站日志时写入对应条目。

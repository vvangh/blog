---
description: vv UI 原子化分层：shadcn-vue / molecules / 业务岛、令牌与状态约定
globs: src/components/**/*,src/composables/**/*,docs/设计/UI组件约定.md,components.json
alwaysApply: false
---

# vv · UI 原子化与组件底座

详细说明见 `docs/设计/UI组件约定.md`。改 UI / 新增组件时必须遵守。

## 分层（禁止反向依赖）

| 层 | 路径 | 规则 |
| --- | --- | --- |
| 原子 | `src/components/ui/*` | shadcn-vue（reka-ui）；只依赖 `reka-ui` / `@/lib/utils` / VueUse；**禁止**依赖 molecules 与业务组件 |
| 分子 | `src/components/molecules/*` | 仅组合原子；无 content collection、无页面业务数据；含 `FilterableSelect` 等共享交互 |
| 有机体 | `src/components/Site*` 等 | 业务岛 / 布局；可依赖 ui + molecules + `@/lib/*` |

- 目录外导入经该目录 `index` barrel + `@/` 或 `~/`
- 新增原子优先：`pnpm dlx shadcn-vue@latest add <name>`，装完核对勿另起色板
- 首屏 Hero / 壁纸视觉约束见 `henglu-blog-brand`（此处只管组件分层）

## 令牌与图标

- 品牌色真相源：`src/styles/tokens.css`（`--hl-*`）
- shadcn 语义色桥接：`src/styles/shadcn.css`（`--primary` 等 → `--hl-*`）
- 图标：`@lucide/vue`（与 `components.json` 的 `iconLibrary` 一致）
- **禁止** Element Plus / Vuetify / Naive 作全站 UI

## 状态

| 场景 | 方案 |
| --- | --- |
| 单岛内 | `ref` / composable / VueUse（**走 auto-import**） |
| 跨岛共享 | `nanostores`（`@/lib/stores`）+ `@nanostores/vue` |
| 默认不用 | Pinia、vue-router |

## 自动导入（硬约定）

`astro.config.mjs` 已挂 `unplugin-auto-import`：`imports: ["vue", "@vueuse/core"]`。

- **禁止**在 `.vue` / 岛内 composable 手写：`import { ref, computed, onMounted, … } from "vue"`
- **禁止**手写：`import { onClickOutside, useX, … } from "@vueuse/core"`（已在 presets 内的一律直接用）
- **允许**：`import type { … } from "vue"`（仅类型）；业务 `@/`、图标 `@lucide/vue`、`@nanostores/vue` 等非 presets 依赖仍显式导入
- 生成类型：`src/types/auto-imports.d.ts`（gitignore；`vp run dev` / `build` 会写出）

## 封装习惯

- 组件 PascalCase；中文描述性注释；做减法
- **单文件 ≤ 500 行**；超限则拆 composable / 子组件 / 样式进 `styles/`
- **样式优先 Tailwind**（布局、间距、字色、圆角）；共享琉璃用已有 `glass-*`；仅 `:deep` / mark / 伪元素等再写少量 CSS
- **关闭 / 清除图标悬停旋转**：统一挂 `motion-safe-icon-spin`（定义在 `src/styles/motion/ambient.css`，降级见 `reduced-motion.css`）；**禁止**组件内私写 `rotate` 悬停
- **多选项下拉**：统一用分子 `FilterableSelect`（`@/components/molecules`）；**在触发框内录入筛选**（Combobox），禁止面板内另开搜索条、禁止不可过滤长列表
- 分子示例：`IconButton`、`LabeledInput`、`FilterableSelect`——新组合放 `molecules/`，不要跨层塞进 `ui/`

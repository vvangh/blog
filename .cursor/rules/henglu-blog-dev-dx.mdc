---
description: vv 开发态所见即所得：搜索与本地验证入口
alwaysApply: true
---

# vv · 开发态所见即所得

## 搜索（Ctrl+K）

- **`vpr dev` / `vp run dev`**：布局注入 content 目录（标题/摘要），改文保存后刷新即可搜，无需先 build。
- **生产全文**：`vp run build` 生成 `dist/pagefind/`（Pagefind）。
- 岛组件：`SiteSearch`；目录构建：`@/lib/search`（`buildDevSearchCatalog`）。

## 本地验证习惯

- 交互/样式：优先 `vpr dev` 目检，不要默认「只有 preview 才能测」。
- e2e / 接近生产：`vp run build` + `vp run test:e2e`（或 preview）。

---
description: 衡录：Node LTS、依赖最新稳定、路径别名与 barrel
alwaysApply: true
---

# 衡录 · 工具链硬约定

## Node

- 项目级 Node = **当前最新 LTS**
- **仅**通过 Vite+ 写入 `package.json`：`vp env pin lts --target dev-engines`
- 真相源：`package.json#devEngines.runtime`（可另有 `engines.node` 作消费者下限；`vp pin` 不改 `engines`）
- **不使用** `.node-version` / `.nvmrc`（有则删；否则 `vp env pin` 会优先改它们）
- 日常：`vp install` / `vp run` / `vp check` 按 pin 解析；核对：`vp env current`

## 依赖

- 安装/升级取 **latest 稳定**，不故意锁旧大版本
- **Vue 固定 3.5.x**（`~3.5`）；**禁止**升级到 3.6 RC，除非计划改口
- 用 `vp add` / `vp install`，不要裸 npm 当日常入口

## 导入路径

| 别名 | 物理路径 |
| --- | --- |
| `@/` | `src/` |
| `~/` | 项目根 |

- 目录外：经该目录 `index`（barrel）+ `@/` 或 `~/` 导入
- **禁止** `../../../` 等深层相对路径；目录内可用浅相对（`./` / `../` 同级）
- 配置：`tsconfig` paths + `astro.config` / `vite.config` 的 `resolve.alias` 三者一致

```ts
// ❌ BAD
import { x } from "../../../lib/site/constants";

// ✅ GOOD
import { x } from "@/lib/site";
```

## 生成类型

- `src/auto-imports.d.ts`、`src/components.d.ts` 为 unplugin 生成物，已 gitignore；本地 `vp run dev` / `vp run build` 会重新写出。

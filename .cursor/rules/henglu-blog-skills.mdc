---
description: vv 博客：技能对照 + 最严边改边审
alwaysApply: true
---

# vv · 技能与边改边审

## 动手前先读 Skill

写/改本仓代码前，先 Read 相关 Skill，禁止凭印象硬写。

### 本仓技能（`.cursor/skills/`）

| 场景 | 技能 |
| --- | --- |
| 发文 / 改 frontmatter / 日记胶片 | `vv-add-content` |
| 多语言文案 / `localePath` / MessageKey | `vv-i18n-ui` |
| 新 `[locale]` 页 / Vue 岛 / molecules | `vv-add-page-island` |
| 单元收口自审 | `vv-self-review` |

### 外部 / 通用技能（有则读；缺失则跳过，勿空转搜仓）

| 场景 | 优先技能 |
| --- | --- |
| 架构 / 分层 / ADR | `jeffallan-claude-skills-architecture-designer` |
| Vue 岛 / SFC | `jeffallan-claude-skills-vue-expert` |
| 页面视觉 / 首屏 | `anthropics-skills-frontend-design` + 规则 `henglu-blog-brand` |
| UX / 无障碍审计 | `jezweb-claude-skills-ux-audit` |
| Playwright / e2e / axe | `jeffallan-claude-skills-playwright-expert` 或 `playwright-testing` |
| `.cursor/rules` 落盘 | `create-rule` |
| 本仓技能落盘 | `create-skill` |
| UI 原子/分子 | 规则 `henglu-blog-ui-kit` + `docs/ui-kit.md` |
| 差分挑错 | `review-bugbot` |
| 安全相关 | `review-security` |

不做：与 Element Plus 后台强绑的技能；无关技能不硬凑。  
规则只放本仓 `blog/.cursor/rules/`，禁止写入 `repository/.cursor`。

## 边改边审

每完成一个可审单元即执行 `vv-self-review`，**不通过不做下一块**。

## 规则地图

| 规则 | always | 管什么 |
| --- | --- | --- |
| `henglu-blog-toolchain` | 是 | Node LTS、`vp`、别名与 barrel |
| `henglu-blog-deps-vp` | 是 | 增删改依赖只许 `vp`（禁裸包管理器/手改） |
| `henglu-blog-scope` | 是 | BACKLOG、分支、不做清单 |
| `henglu-blog-i18n-routing` | 是 | `base`、`localePath`、messages |
| `henglu-blog-skills` | 是 | 本表 |
| `henglu-blog-dev-dx` | 是 | 开发态搜索与验证入口（短） |
| `henglu-blog-ui-kit` | 否 | 原子/分子/岛、令牌、auto-import |
| `henglu-blog-content` | 否 | Content Collections 与发文 |
| `henglu-blog-brand` | 否 | vv Air 视觉（不含 UI 控件细则） |
| `henglu-blog-astro-islands` | 否 | Astro vs 岛、boot、BASE_URL |
| `henglu-blog-testing` | 否 | Vitest / Playwright / axe |

面向人的文本用简体中文。细节以各规则正文为准。

---
/** 建站日志列表占位（M5 填充）。 */
import SiteLayout from "@/layouts/SiteLayout";
---

<SiteLayout title="从零到一">
  <h1 class="text-3xl font-semibold">从零到一</h1>
  <p class="mt-4 opacity-80">建站日志将写入 Content Collection「build-log」，并在此公开。</p>
</SiteLayout>
